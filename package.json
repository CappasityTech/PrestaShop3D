{
  "name": "@cappasity/prestahop",
  "private": true,
  "repository": "git@github.com:CappasityInc/prestashop-module-gallery.git",
  "scripts": {
    "semantic-release": "yarn --frozen-lockfile; semantic-release",
    "nodemon": "nodemon",
    "dev": "./scripts/development-server.sh",
    "build": "./scripts/prepare.sh"
  },
  "devDependencies": {
    "@semantic-release/exec": "^3.3.7",
    "nodemon": "^1.19.3",
    "semantic-release": "^15.13.24"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -e $HUSKY_GIT_PARAMS"
    }
  },
  "dependencies": {
    "@commitlint/cli": "^8.2.0",
    "husky": "^3.0.7"
  }
}
