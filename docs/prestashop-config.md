# Prestashop configuration

- Overrides should be enabled. See "Advanced Parameters -> Performance", "Disable all overrides" option must be set to "No".
![disable overrides](images/disable-overides.png)
